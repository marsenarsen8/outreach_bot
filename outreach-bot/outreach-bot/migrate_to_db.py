#!/usr/bin/env python3
"""
Скрипт для миграции данных из CSV файлов в базу данных SQLite
"""

import os
import sys
import pandas as pd
from pathlib import Path

# Добавляем текущую директорию в путь для импорта
sys.path.append(os.path.dirname(os.path.abspath(__file__)))

import database_functions

def migrate_contacts():
    """Миграция контактов из CSV в базу данных"""
    csv_file = 'test base.csv'
    
    if not os.path.exists(csv_file):
        print(f"❌ Файл {csv_file} не найден")
        return False
    
    try:
        print(f"📥 Импортируем контакты из {csv_file}...")
        imported_count = database_functions.import_contacts_from_csv(csv_file)
        print(f"✅ Импортировано {imported_count} контактов")
        return True
    except Exception as e:
        print(f"❌ Ошибка импорта контактов: {e}")
        return False

def migrate_results():
    """Миграция результатов из CSV в базу данных"""
    csv_file = 'results.csv'
    
    if not os.path.exists(csv_file):
        print(f"⚠️ Файл {csv_file} не найден, пропускаем")
        return True
    
    try:
        print(f"📥 Импортируем результаты из {csv_file}...")
        imported_count = database_functions.import_results_from_csv(csv_file)
        print(f"✅ Импортировано {imported_count} результатов")
        return True
    except Exception as e:
        print(f"❌ Ошибка импорта результатов: {e}")
        return False

def migrate_knowledge_base():
    """Миграция базы знаний из файлов в базу данных"""
    knowledge_dir = 'knowledge_base'
    
    if not os.path.exists(knowledge_dir):
        print(f"⚠️ Папка {knowledge_dir} не найдена, пропускаем")
        return True
    
    try:
        print(f"📥 Импортируем базу знаний из {knowledge_dir}...")
        imported_count = database_functions.import_knowledge_from_files(knowledge_dir)
        print(f"✅ Импортировано {imported_count} файлов базы знаний")
        return True
    except Exception as e:
        print(f"❌ Ошибка импорта базы знаний: {e}")
        return False

def create_default_prompts():
    """Создание промптов по умолчанию"""
    try:
        print("📝 Создаем промпты по умолчанию...")
        
        # Подключаемся к базе данных
        import sqlite3
        conn = sqlite3.connect(database_functions.DB_PATH)
        cursor = conn.cursor()
        
        # Проверяем, есть ли уже промпты
        cursor.execute('SELECT COUNT(*) FROM prompts')
        if cursor.fetchone()[0] > 0:
            print("⚠️ Промпты уже существуют, пропускаем")
            conn.close()
            return True
        
        # Создаем промпты по умолчанию
        default_prompts = [
            {
                'name': 'Основной промпт',
                'content': """Всегда отвечай на русском языке. Ты Роллан — виртуальный менеджер по продажам в компании bored.kz. Мы разрабатываем сайты, интернет-магазины, веб-сервисы, порталы и делаем UX/UI-дизайн. Цены начинаются от 50 000 тенге.

Ты пишешь первым «холодному» контакту, который ничего о нас не знает. Твоя задача — вежливо представиться, кратко и понятно рассказать, чем занимается компания, и предложить сотрудничество.

💬 Правила:
- Пиши в дружелюбном, неформальном, но уважительном тоне.
- Используй emoji в начале или конце.
- Не перечисляй все услуги и не пиши длинный текст — будь лаконичным.
- Обязательно задай уточняющий вопрос: например, занимается ли человек бизнесом, интересен ли ему сайт, нужен ли новый визуал.
- Если человек отказывается — поблагодари за ответ и больше не пиши.
- Если заинтересован — уточни, какой сайт может понадобиться, и предложи передать его контакт менеджеру для консультации.
- Используй информацию из базы знаний для более точных и убедительных ответов.

✏️ Задача: сгенерируй ОДНО короткое и цепляющее приветственное сообщение для первого контакта с холодным клиентом. Включи:
1. Приветствие с именем (если есть),
2. Кто ты и чем занимается bored.kz,
3. Предложение сотрудничества (создание сайта, визуал, онлайн-платформа),
4. Вопрос для вовлечения (например, «занимаетесь бизнесом?» или «было бы актуально обсудить сайт?»).

Не повторяй примеры, просто выдай один готовый текст, как для отправки в Telegram.""",
                'description': 'Основной промпт для первого контакта с клиентами',
                'is_active': True
            },
            {
                'name': 'Диалоговый промпт',
                'content': """Ты ведёшь переписку с потенциальными клиентами в Telegram от имени компании bored.kz. Мы делаем сайты, интернет-магазины, веб-сервисы и UX/UI-дизайн. Цены от 50 000 тенге.

Ты отвечаешь на «холодные» входящие сообщения. Люди часто присылают сразу несколько сообщений подряд. Не нужно реагировать на каждое — **дождись примерно минуты с момента последнего сообщения** и **ответь один раз, обобщив всю информацию**.

📌 В ответе:
- Пиши кратко и дружелюбно.
- Не повторяйся.
- Отвечай только одним сообщением.
- Не используй подпись или указание, кто ты (не пиши "Роллан:" и т.п.).
- Включи 1–2 вопроса по теме: например, какой сайт нужен, в какой сфере работает клиент, есть ли пожелания по дизайну.
- Если клиент заинтересован — предложи передать контакт менеджеру для консультации.
- Если клиент отказывается — поблагодари и больше не пиши.
- Используй техники продаж и аргументы из базы знаний для работы с возражениями.

💡 Цель: заинтересовать клиента и выяснить, нужен ли сайт или визуал. Общайся живо, уважительно и по делу.""",
                'description': 'Промпт для ведения диалогов с клиентами',
                'is_active': False
            }
        ]
        
        for prompt in default_prompts:
            cursor.execute('''
                INSERT INTO prompts (name, content, description, is_active)
                VALUES (?, ?, ?, ?)
            ''', (prompt['name'], prompt['content'], prompt['description'], prompt['is_active']))
        
        conn.commit()
        conn.close()
        
        print("✅ Промпты по умолчанию созданы")
        return True
    except Exception as e:
        print(f"❌ Ошибка создания промптов: {e}")
        return False

def main():
    """Основная функция миграции"""
    print("🚀 Начинаем миграцию данных в базу данных SQLite...")
    
    # Инициализируем базу данных
    print("📊 Инициализируем базу данных...")
    database_functions.init_database()
    
    # Мигрируем данные
    success = True
    
    success &= migrate_contacts()
    success &= migrate_results()
    success &= migrate_knowledge_base()
    success &= create_default_prompts()
    
    if success:
        print("\n🎉 Миграция завершена успешно!")
        print("\n📋 Что было сделано:")
        print("- Создана база данных SQLite с таблицами для контактов, результатов, промптов и базы знаний")
        print("- Импортированы контакты из CSV файла")
        print("- Импортированы результаты диалогов (если есть)")
        print("- Импортирована база знаний из файлов")
        print("- Созданы промпты по умолчанию")
        print("\n✅ Теперь бот и веб-панель будут работать с базой данных!")
    else:
        print("\n❌ Миграция завершена с ошибками!")
        print("Проверьте логи выше и исправьте ошибки.")

if __name__ == "__main__":
    main() 